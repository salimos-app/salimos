services:
  - type: web
    name: salimos
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node proxy-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
